# Required in local development and Vercel for persistent enquiries and future application data.
# Use the Neon PostgreSQL connection string. Never commit a real value.
DATABASE_URL=

# Optional. Vercel supplies its production URL automatically; set this only for a custom canonical URL.
NEXT_PUBLIC_SITE_URL=
